{
  "name": "used-items-marketplace",
  "version": "1.0.0",
  "description": "Клиентская часть интернет-ресурса для размещения объявлений о продаже подержанных вещей",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .js",
    "format": "prettier --write src",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "gh-pages": "^6.1.1"
  }
}
